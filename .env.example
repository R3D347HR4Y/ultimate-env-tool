# Production site origin (no trailing slash). Used for canonical URL, Open Graph image URL, robots.txt, and sitemap.xml.
# If unset in a production build, defaults to https://envtool.eliott.cloud (dev server still uses http://localhost:5188).
# Example: https://env.example.com
VITE_SITE_URL=
